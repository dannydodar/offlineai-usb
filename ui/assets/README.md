# Dangle boot asset

The supplied transparent logo is stored as `ui/assets/old-logo.png`.

The boot sequence shows it on the opening white frame, fragments it, and transitions into the DANGLE data field. If the asset is absent during development, the marked fallback in `index.html` can be enabled temporarily; it is not the intended final asset.
